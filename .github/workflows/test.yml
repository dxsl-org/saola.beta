name: test

on:
  push:
    branches:
      - main
      - develop
  pull_request:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: erlef/setup-beam@v1.24.0
        with:
          otp-version: "28"
          gleam-version: "1.17.0"
          rebar3-version: "3"
          # elixir-version: "1"
      - uses: oven-sh/setup-bun@v2.2.0
      - run: gleam deps download
      # Tests import FFI wrappers (world-map → d3); node deps must be present
      - run: bun install
      - run: gleam test
      - run: gleam format --check src test dev
